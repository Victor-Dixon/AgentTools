# Imports

This folder stores file manifests only.

Raw source snapshots are intentionally not copied yet because the source repositories contain many sensitive-name hits and archive/workspace files.

Next gate: classify extraction candidates and copy only approved files.
