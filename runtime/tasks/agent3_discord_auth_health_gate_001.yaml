task_id: agent3_discord_auth_health_gate_001
title: Add Discord auth health gate without exposing secrets
owner: Cursor Agent-3
status: ready
priority: medium
repo: D:/agent-tools
branch_recommendation: ops/agent3-discord-auth-health
context:
  current_error: Invalid Discord token
  guild_id_status: DISCORD_GUILD_ID is now set at User scope
scope:
  - find bot startup path for run_unified_discord_bot_with_restart.py
  - add preflight auth/env validation if missing
  - distinguish MISSING_TOKEN, INVALID_TOKEN, MISSING_GUILD_ID
  - make restart wrapper stop noisy restart loops on invalid token
  - write redacted health report
out_of_scope:
  - do not print or store token
  - do not ask user for token inside code
  - do not modify Discord Developer Portal
verification:
  - invalid token produces clear FAIL_INVALID_DISCORD_TOKEN
  - missing guild produces FAIL_MISSING_DISCORD_GUILD_ID
  - valid-looking env proceeds to login attempt
  - no secrets appear in logs
done_when:
  - bot auth failures are deterministic and non-spammy
  - operator knows exactly what human action is required
