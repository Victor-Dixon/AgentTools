You are Cursor Agent-2.

TARGET:
Wire AgentTools swarm knowledge search to the dreamos-brain advisory API.

dreamos-brain now provides read-only advisory endpoints:
- GET /advisory/health
- GET /advisory/manifest
- GET /advisory/search?q=&limit=20
- GET /advisory/entries/{entry_id}

Default API base:
  http://127.0.0.1:8000

Environment override:
  DREAMOS_BRAIN_URL

Rules:
- Do not delete local AgentTools knowledge files.
- Do not relocate MCP runtime yet.
- Add proxy/bridge first.
- Preserve advisory/non-canonical semantics.
- Use local fallback only when API is unavailable.
- Do not make search write to dreamos-brain.
- share_learning can remain local/deferred unless there is already a safe endpoint.

Verification:
1. Start dreamos-brain locally:
   cd D:\dreamos-brain
   uvicorn app.main:app --host 127.0.0.1 --port 8000

2. From AgentTools, verify search_swarm_knowledge proxies to:
   /advisory/search?q=MCP&limit=5

3. Stop API and verify fallback does not crash.

4. Write:
   runtime/reports/proxy_agenttools_swarm_knowledge_to_dreamos_brain_001.txt

Required report marker:
  VERIFY=PASS_AGENTTOOLS_SWARM_KNOWLEDGE_PROXY

Final response:
Task:
Actions Taken:
Verification:
Files Changed:
Commit Message:
Status:
