# Legacy Surfaces Archive

These directories were moved out of the canonical AgentTools root because they are standalone or side-product surfaces, not the core toolbelt/MCP runtime.

Canonical active surfaces remain:

- `tools_v2/`
- `swarm_mcp/`
- `mcp_servers/`
- `apps/`
- `packages/`
- `tests/`
- `docs/`

Archived surfaces preserve history and can be extracted into separate repositories later if needed.
