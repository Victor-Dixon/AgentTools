# Legacy Reports Archive

Bulk historical reports were moved out of active tool paths to reduce root and `tools/` noise.

This archive is non-runtime documentation. New operational reports should go under `docs/`, `_ops/`, or a dated release/status folder, not mixed into executable tool directories.
