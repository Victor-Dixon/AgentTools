# Integration sequence

```text
AgentTools repo facts
    -> ProjectScanner hygiene aggregation
    -> DreamVault policy
    -> CPC authorized mutation
```

The sequence is intentionally fail-closed. AgentTools and ProjectScanner remain read-only in this lane.
